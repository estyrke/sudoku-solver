{
  "name": "sudoku-solver-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The puzzle helper. An ordinary Vite project: web/ is the root, public/ holds the assets copied through to the site root, and `vite build` emits dist/ — the whole deployable site, gitignored and built on deploy. There is no server.",
  "scripts": {
    "build": "vite build",
    "dev": "vite --port 8124 --strictPort",
    "serve": "vite preview --port 8124 --strictPort",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^26.5.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "preact": "^10.29.8"
  }
}
