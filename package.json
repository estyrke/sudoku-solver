{
  "name": "sudoku-solver-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TypeScript front end for the puzzle helper. Vite compiles web/*.ts into static/dist/, which is what index.html loads and what the jsdom page tests import.",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^26.5.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "preact": "^10.29.8"
  }
}
