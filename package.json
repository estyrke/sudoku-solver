{
  "name": "sudoku-solver-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The puzzle helper. Vite compiles web/*.ts into static/dist/, which is what index.html loads and what the jsdom page tests import; the bundle is built rather than committed, so `npm run build` comes before those tests and before a deploy. The app ships as the static files in static/ and there is no server.",
  "scripts": {
    "build": "vite build",
    "dev": "vite --port 8124 --strictPort",
    "serve": "node tools/serve.mjs 8124",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^26.5.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "preact": "^10.29.8"
  }
}
